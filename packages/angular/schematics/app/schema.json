{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsApp",
  "title": "Mockly App Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "Mockly app name",
      "type": "string",
      "x-promt": "What will be the app name?"
    },
    "path": {
      "description": "App parent folder",
      "type": "string"
    }
  },
  "required": [
    "name"
  ]
}
